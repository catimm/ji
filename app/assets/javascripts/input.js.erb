$(document).ready(function(){

		CameraTag.observe('jitest', "initialized", function(){
			alert("test");
			alert("current user is"+gon.current_user);
			myCamera = CameraTag.cameras['jitest'];

			var js_object = {
			    camera: "jitest", 
			    user: "12"
			};
			myCamera.addVideoData(js_object);
		});
		
});